{
  "name": "threejs-animation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf .cache/ dist/ build/",
    "start": "parcel --no-hmr index.html",
    "prestart": "mkdir -p dist/assets/ && cp -r src/assets/* dist/assets/",
    "build": "parcel build --public-url ./ -d build index.html",
    "prebuild": "mkdir -p build/assets/ && cp -r src/assets/* build/assets/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "1.11.0",
    "sass": "^1.15.2"
  },
  "dependencies": {
    "easing-utils": "0.0.7",
    "eventemitter3": "3.1.0",
    "microlight": "0.0.7",
    "three": "0.98.0"
  }
}
