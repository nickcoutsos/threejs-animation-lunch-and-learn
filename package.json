{
  "name": "threejs-animation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf .cache/ dist/ build/",
    "start": "parcel --no-hmr index.html",
    "prestart": "mkdir -p dist/",
    "build": "parcel build --public-url ./ --no-minify -d build index.html",
    "prebuild": "mkdir -p build/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "1.11.0",
    "sass": "^1.15.2"
  },
  "dependencies": {
    "easing-utils": "0.0.7",
    "eventemitter3": "3.1.0",
    "microlight": "0.0.7",
    "three": "0.99.0"
  }
}
